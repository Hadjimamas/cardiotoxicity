name: cardiotoxicity
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.0
  cupertino_icons: ^1.0.5
  sqflite: ^2.2.8+2
  syncfusion_flutter_charts: ^21.2.4
  path: any
  path_provider: ^2.0.15
  sparkline: ^1.0.0
  flutter_staggered_grid_view: ^0.6.2
  http: ^0.13.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
  #Adding the JSON Files into an asset folder and declaring the directory so I can use them to my project
  assets:
    - assets/
    - assets/response_calories_steps.json
    - assets/heartrate.json
